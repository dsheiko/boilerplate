// The default.js file is used to render a fallback within Parallel Routes 
// when Next.js cannot recover a slot's active state after a full-page load.
export default function Default() {
  return null;
}